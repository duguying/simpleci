{{define "header"}}
    <div class="header">
        <span>Blog</span>
        <span>Status</span>
        <span>Changelog</span>
        <a class="exit-button" href="/user/logout">退出</a>
        <a class="login-user" href="/home/">{{.username}}</a>
    </div>
{{end}}
